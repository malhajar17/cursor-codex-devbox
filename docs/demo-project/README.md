# Demo project

Harmless sample content for documentation screenshots. It contains no credentials, network calls, or external dependencies.

Open `src/greeting.py` beside Codex, then drag the `src` folder into the Codex input. Check for a folder attachment without submitting a prompt.
